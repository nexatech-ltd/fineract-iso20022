spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test_iso20022?useSSL=false&allowPublicKeyRetrieval=true
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  data:
    redis:
      host: localhost
      port: 6379
  kafka:
    bootstrap-servers: localhost:9092

fineract:
  api:
    base-url: http://localhost:8443/fineract-provider/api/v1
    username: mifos
    password: password
    tenant-id: default

iso20022:
  kafka:
    inbound-topic: test.iso20022.inbound
    outbound-topic: test.iso20022.outbound
    status-topic: test.iso20022.status
    dlq-topic: test.iso20022.dlq
  cache:
    idempotency-ttl: 300
    account-cache-ttl: 60

logging:
  level:
    org.fineract.iso20022: DEBUG
